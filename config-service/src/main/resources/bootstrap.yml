server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/digitallab-academy/ms-course-youtube.git
          search-paths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSW}
  security:
    user:
      name: root
      password: s3cr3t
